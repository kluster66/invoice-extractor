# Configuration AWS (optionnel - utilise AWS CLI par défaut)
# AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=votre_access_key_id
# AWS_SECRET_ACCESS_KEY=votre_secret_access_key
# AWS_SESSION_TOKEN=votre_session_token_optionnelle

# Configuration Bedrock
# Modèles disponibles : 
# - meta.llama3-1-70b-instruct-v1:0 (recommandé - pas d'activation requise)
# - anthropic.claude-3-5-sonnet-20241022-v2:0 (meilleure précision - activation requise)
# - anthropic.claude-3-haiku-20240307-v1:0 (rapide et économique)
# - amazon.titan-text-express-v1 (natif AWS)
BEDROCK_MODEL_ID=meta.llama3-1-70b-instruct-v1:0
BEDROCK_MAX_TOKENS=1000
BEDROCK_TEMPERATURE=0.1

# Configuration DynamoDB
DYNAMODB_TABLE_NAME=invoices
DYNAMODB_READ_CAPACITY=5
DYNAMODB_WRITE_CAPACITY=5

# Configuration S3
S3_INPUT_BUCKET=factures-{votre-account-id}-us-west-2
S3_PROCESSED_PREFIX=processed/
S3_ERROR_PREFIX=error/

# Configuration Application
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
MAX_PDF_SIZE_MB=50
TEMP_DIR=/tmp
EXTRACTION_TIMEOUT=300  # 5 minutes en secondes
MAX_RETRY_ATTEMPTS=3

# Notes :
# 1. L'application utilise AWS CLI par défaut (aws configure)
# 2. Pour changer de modèle : Config.set_model('claude-3-haiku')
# 3. Pour tester : python test_models_simple.py
# 4. Pour lister les modèles : python list_available_models.py
